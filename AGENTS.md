# Codex 작업 규칙

이 파일의 규칙은 프로젝트 루트와 모든 하위 폴더에 적용됩니다.

## 자동 Git 커밋

- 이 프로젝트의 승인된 push 대상은 `origin`의 `git@github.com:nyah-10/NYxTPT_Prototype.git`입니다. push 전에 `git remote get-url origin`으로 정확히 일치하는지 확인하고, 일치하면 현재 추적 브랜치에 일반 push를 수행합니다. 다른 주소이거나 `origin`이 없으면 push하지 말고 사용자에게 알립니다.
- Windows에서 `git` 명령이 PATH에서 발견되지 않으면 작업을 포기하지 말고, `C:\Program Files\Git\cmd\git.exe`, `%LOCALAPPDATA%\Programs\Git\cmd\git.exe`, `%LOCALAPPDATA%\Programs\MinGit\cmd\git.exe` 순서로 확인하여 사용합니다.
- Codex가 파일을 생성하거나 수정하는 작업을 성공적으로 완료하고 필요한 검증까지 통과하면, 사용자에게 결과를 전달하기 전에 자동으로 Git 커밋을 생성합니다.
- 커밋에는 현재 작업에서 Codex가 생성하거나 수정한 파일만 포함합니다. 작업 시작 전에 존재하던 사용자 변경이나 현재 작업과 무관한 변경은 staging하거나 커밋하지 않습니다.
- 커밋 메시지는 변경 목적을 짧고 구체적으로 설명합니다.
- 읽기 전용 조사, 설명, 진단만 수행한 경우에는 커밋하지 않습니다.
- 구현이나 검증이 실패했거나 작업이 미완료된 경우에는 자동 커밋하지 않습니다.
- 사용자가 해당 작업에서 커밋하지 말라고 요청하면 그 요청을 우선합니다.
- 자동 커밋이 성공하면 현재 브랜치의 추적 원격 저장소로 일반 push를 자동 실행합니다.
- force push와 원격 이력 재작성은 자동으로 수행하지 않습니다.
- 원격 저장소가 없거나 인증, 네트워크, 원격 변경 때문에 push가 실패하면 로컬 커밋은 그대로 보존하고 실패 원인을 사용자에게 알립니다.
- 커밋 전에 `git diff`와 `git status`를 확인하여 비밀정보, 캐시, 빌드 결과물 및 무관한 파일이 포함되지 않았는지 검토합니다.

## Unity 검증

- `Temp/UnityLockfile` 또는 실행 중인 Unity Editor로 현재 프로젝트가 열려 있으면 동일 프로젝트에 대한 Unity `-batchmode` 컴파일을 시도하지 않습니다.
- Unity Editor가 열려 있다는 이유만으로 구현 작업 전체를 실패 또는 미완료로 처리하지 않습니다. 가능한 정적 검사와 변경 범위 검증을 수행한 뒤, 코드상 문제가 없으면 자동 커밋과 push를 계속하고 배치 컴파일을 생략했다고 결과에 명시합니다.
- Unity 컴파일 결과가 반드시 필요한 변경이고 다른 검증으로 대체할 수 없을 때만 사용자에게 Editor를 닫아 달라고 요청합니다.
- Unity Editor를 강제 종료하거나 `UnityLockfile`을 임의로 삭제하지 않습니다.
